{# empty Twig template #}
{% extends "::base.html.twig"%}

{% block body %}
    {#<div class='container signup'>
        <form class="form-signup" role="form" action="{{ path('_signup') }}" method="post">
            <h2 class="form-signup-heading">Please sign up</h2>
            <label class="sr-only" for="inputEmail">Email address</label>
            <input id="inputEmail" class="form-control" autofocus="" type="email" required="" placeholder="Email address" name="_username">
            <label class="sr-only" for="inputPassword">Password</label>
            <input id="inputPassword" class="form-control" type="password" required="" placeholder="Password" name="_password">
            
            <button class="btn btn-lg btn-primary btn-block" type="submit">Sign up</button>
        </form>#}
        <form action="{{ path('_create')}}" method="post" {{ form_enctype(form) }}>
            {{ form_widget(form) }}

            <input type="submit" />
        </form>
    </div>
{% endblock %}